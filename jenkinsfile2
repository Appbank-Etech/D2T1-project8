pipeline{
    agent any
    stages{
        stage('1-system processes Tunde'){
            steps{
                sh 'ps -ef'
                sh 'sudo systemctl status jenkins'
            }
        }
        stage('2-system processes Mercy){
            steps{
                sh 'ps -ef'
                sh 'sudo systemctl status jenkins'
            }
        }
        stage('3-Lydia system processes){
            steps{
                sh 'ps -ef'
                sh 'sudo systemctl status jenkins'
            }
        }
        stage('4-Juliet system processes'){
            steps{
                sh 'ps -ef'
                sh 'sudo systemcl status jenkins'
            }
         }
         stage('5-Chris system processes'){
            steps{
                sh 'ps -ef'
                sh 'sudo systemctl status jenkins'
             }
         }
    }
}
