pipeline{
    agent any
    stages{
        stage('1-system processes Tunde'){
            steps{
                sh 'ps -ef'
                sh 'sudo systemctl jenkins status'
            }
        }
        stage('2-system process mercy){
            steps{
                sh 'ps -ef'
                sh 'sudo systemctl jenkins status'
            }
        }
    }
}
